@using Microsoft.AspNetCore.Identity
@using HumanMusicSchoolManager.Models

@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager
<!-- Sidebar -->
<ul class="sidebar navbar-nav">
    <li class="nav-item">
        <a class="nav-link" asp-area="" asp-controller="Home" asp-action="Index">
            <i class="fas fa-fw fa-tachometer-alt"></i>
            <span>Dashboard</span>
        </a>
    </li>
    @if (SignInManager.IsSignedIn(User))
    {
        @if (!(User.IsInRole("Professor") || User.IsInRole("Aluno")))
        {
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="pagesDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <i class="fas fa-chalkboard-teacher"></i>
                    <span>Professor</span>
                </a>
                <div class="dropdown-menu" aria-labelledby="pagesDropdown">
                    <a class="dropdown-item" asp-controller="Professor" asp-action="Index">Lista</a>
                    @if (User.IsInRole("Secretaria") || User.IsInRole("Admin"))
                    {
                        <a class="dropdown-item" asp-controller="Professor" asp-action="Form">Cadastrar</a>
                    }
                </div>
            </li>
        }
        @if (!(User.IsInRole("Professor") || User.IsInRole("Aluno")))
        {
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="pagesDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <i class="fas fa-music"></i>
                    <span>Curso</span>
                </a>
                <div class="dropdown-menu" aria-labelledby="pagesDropdown">
                    <a class="dropdown-item" asp-controller="Curso" asp-action="Index">Lista</a>
                    @if (User.IsInRole("Secretaria") || User.IsInRole("Admin"))
                    {
                        <a class="dropdown-item" asp-controller="Curso" asp-action="Form">Cadastrar</a>
                    }
                </div>
            </li>
        }
        @if (!(User.IsInRole("Professor") || User.IsInRole("Aluno") || User.IsInRole("Financeiro")))
        {
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="pagesDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <i class="fas fa-user-graduate"></i>
                    <span>Aluno</span>
                </a>
                <div class="dropdown-menu" aria-labelledby="pagesDropdown">
                    <a class="dropdown-item" asp-controller="Aluno" asp-action="Index">Lista</a>
                    <a class="dropdown-item" asp-controller="Aluno" asp-action="Form">Cadastrar</a>
                </div>
            </li>
        }
        @if (!(User.IsInRole("Professor") || User.IsInRole("Aluno") || User.IsInRole("Financeiro")))
        {
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="pagesDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <i class="fas fa-chalkboard"></i>
                    <span>Sala</span>
                </a>
                <div class="dropdown-menu" aria-labelledby="pagesDropdown">
                    <a class="dropdown-item" asp-controller="Sala" asp-action="Index">Lista</a>
                    <a class="dropdown-item" asp-controller="Sala" asp-action="Form">Cadastrar</a>
                </div>
            </li>
        }
        @if (!(User.IsInRole("Professor") || User.IsInRole("Aluno") || User.IsInRole("Financeiro") || User.IsInRole("Atendimento")))
        {
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="pagesDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <i class="far fa-address-card"></i>
                    <span>Funcionário</span>
                </a>
                <div class="dropdown-menu" aria-labelledby="pagesDropdown">
                    <a class="dropdown-item" asp-controller="Funcionario" asp-action="Index">Lista</a>
                    <a class="dropdown-item" asp-controller="Funcionario" asp-action="Form">Cadastrar</a>
                </div>
            </li>
        }
        <li class="nav-item">
            <a class="nav-link" href="charts.html">
                <i class="fas fa-fw fa-chart-area"></i>
                <span>Charts</span>
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="tables.html">
                <i class="fas fa-fw fa-table"></i>
                <span>Tables</span>
            </a>
        </li>
    }
</ul>


@*@if (SignInManager.IsSignedIn(User))
    {
        if (User.IsInRole("Professor"))
        {
            <li><a asp-area="" asp-controller="diarioClasse" asp-action="Index">Diário de Classe</a></li>
        }
        if (User.IsInRole("Admin") || User.IsInRole("Coordenacao"))
        {
            <li><a asp-area="" asp-controller="professor" asp-action="Index">Professores</a></li>
        }
        if (User.IsInRole("Admin") || User.IsInRole("Coordenacao") || User.IsInRole("Professor"))
        {
            <li><a asp-area="" asp-controller="aluno" asp-action="Index">Alunos</a></li>
        }
        if (User.IsInRole("Admin") || User.IsInRole("Coordenacao"))
        {
            <li><a asp-area="" asp-controller="curso" asp-action="Index">Cursos</a></li>
        }
        if (User.IsInRole("Admin"))
        {
            <li><a asp-area="" asp-controller="funcionario" asp-action="Index">Funcionário</a></li>
        }
        if (User.IsInRole("Admin"))
        {
            <li><a asp-area="" asp-controller="user" asp-action="Index">Usuários</a></li>
        }
    }
    else
    {

    }*@