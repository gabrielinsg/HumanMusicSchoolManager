@model List<Demostrativa>
@{
    ViewData["Title"] = "DemostrativasAbertas";
}

<div class="card mb-3">
    <div class="card-header">
        <div class="row">
            <div class="col-xl-11 col-md-10">
                <i class="fas fa-music"></i>
                Demostrativas abertas
            </div>
        </div>

    </div>
    <div class="card-body">
        <div class="table-responsive table table-sm table-hover small">
            <table class="table table-bordered table-striped" id="dataTable" cellspacing="0">
                <thead>
                    <tr class="small">
                        <th class="text-center">Data</th>
                        <th>Nome</th>
                        <th>Curso</th>
                        <th>Professor</th>
                        <th>Marcada por</th>
                        <th>Confirmada</th>
                        <th>Aula dada</th>
                        <th>Presença</th>
                        <th>Email</th>
                        <th>Telefone</th>
                        <th>Celular</th>
                        <th class="text-center">Demostrativa</th>
                    </tr>
                </thead>
                <tfoot>
                    <tr class="small">
                        <th class="text-center">Data</th>
                        <th>Nome</th>
                        <th>Curso</th>
                        <th>Professor</th>
                        <th>Marcada por</th>
                        <th>Confirmada</th>
                        <th>Aula dada</th>
                        <th>Presença</th>
                        <th>Email</th>
                        <th>Telefone</th>
                        <th>Celular</th>
                        <th class="text-center">Demostrativa</th>
                    </tr>
                </tfoot>
                <tbody>
                    @foreach (var Demostrativa in Model)
                    {
                        <tr>
                            <td class="text-center">@Demostrativa.Aula.Data.ToString("dd/MM/yyy - HH:mm'h'")</td>
                            <td>@Demostrativa.Candidato.Nome</td>
                            <td>@Demostrativa.Curso.Nome</td>
                            <td>@Demostrativa.DispSala.Professor.Nome</td>
                            <td>@Demostrativa.Pessoa.Nome</td>
                            <td>
                                <select asp-for="@Demostrativa" id="confirm@(Demostrativa.Id)" onchange="Confimacao('confirm@(Demostrativa.Id)',@Demostrativa.Id)" class="form-control" asp-items="Html.GetEnumSelectList<Confirmado>()" >
                                    <option value="@Demostrativa.Confirmado">@Html.DisplayFor(d => Demostrativa.Confirmado)</option>
                                </select>
                            </td>
                            <td>@(Demostrativa.Aula.AulaDada == true ? "Sim" : "Não")</td>
                            <td>@(Demostrativa.Presenca == null ? "" : Demostrativa.Presenca == true ? "Presente" : "Faltou")</td>
                            <td>@Demostrativa.Candidato.Email</td>
                            <td>@Demostrativa.Candidato.Tel</td>
                            <td>@Demostrativa.Candidato.Cel</td>
                            <td class="text-center"><a asp-controller="Candidato" asp-action="Candidato" asp-route-candidatoId="@Demostrativa.CandidatoId"><i class="fas fa-music"></i></a></td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@section Scripts{
    <script src="~/vendor/datatables/jquery.dataTables.js"></script>
    <script src="~/vendor/datatables/dataTables.bootstrap4.js"></script>
    <script src="~/js/demo/datatables-demo.js"></script>
    <script src="~/js/Demostrativa.js"></script>
}

